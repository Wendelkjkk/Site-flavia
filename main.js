function enviarMensagem() {
    const usuarioInstagram = "flavia0xz"; // Nome de usuário do Instagram sem '@'
    const linkInstagram = `https://www.instagram.com/${usuarioInstagram}/`; // Link para o perfil do Instagram
    
    // Abrir o perfil do Instagram do usuário em uma nova aba
    window.open(linkInstagram, '_blank');
}
